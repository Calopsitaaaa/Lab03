#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
float num, ra;
printf("digite um número: ");
scanf("%f", &num);
if(num >=0)
{
raiz = sqrt(num);
printf("raiz: %f", rai;
}
else
{
printf("numero inválido");
}
return 0};

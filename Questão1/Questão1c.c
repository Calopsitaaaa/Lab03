#include <stdio.h>
#include <stdlib.h>
int main(){
    int numA, numB;
    printf("coloque o primeiro numero");
    scanf("%d", &numA);
    printf("coloque o segundo numero");
    scanf("%d", &numB);
    if(numA>numB){
        printf("%d", numA);
    else}
    (numB>numA)
        printf("%d", numB);
    }
    system("PAUSE");
}
